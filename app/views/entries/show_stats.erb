<!doctype html>

<html lang = "en">
<head>
  <meta charset="UTF-8">
  <script src="http://cdnjs.cloudflare.com/ajax/libs/modernizr/2.7.1/modernizr.min.js"></script>
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/3.0.3/normalize.min.css">
  <link rel="stylesheet" type="text/css" href="/style.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0">
</head>

<body class = "maxout3">

<h1 class = "show_stats">DAMN BRO! NOT BAD!</h1>

<img src="/bl.jpg" alt="bostin" style="width:160px;height:150px;">

<br></br>

<% @entry.exercise_entries.each do |entry| %>
  <% if Exercise.find_by_id(entry.exercise_id).name == "Biceps Curl" %>
  <strong><%= Exercise.find_by_id(entry.exercise_id).name %></strong>
  <br></br>
    Weight: <%= entry.weight %>
    Reps: <%= entry.reps %>
<% end %>
<% end %>

  <br></br>

  <% @entry.exercise_entries.each do |entry| %>
  <% if Exercise.find_by_id(entry.exercise_id).name == "Leg Extension" %>
  <strong><%= Exercise.find_by_id(entry.exercise_id).name %></strong>
   <br></br>
     Weight: <%= entry.weight %>
     Reps: <%= entry.reps %>
    <% end %>
    <% end %>

      <br></br>

  <% @entry.exercise_entries.each do |entry| %>
  <% if Exercise.find_by_id(entry.exercise_id).name == "Chest Press" %>
  <strong><%= Exercise.find_by_id(entry.exercise_id).name %></strong>
  <br></br>
    Weight: <%= entry.weight %>
    Reps: <%= entry.reps %>
  <% end %>
  <% end %>

        <br></br>

<a href="/edit_entry/<%= @entry.id %>">EDIT</a>

<br></br>

<a href="/entries">VIEW ALL WORKOUT SESSIONS</a>

</body>

</html>